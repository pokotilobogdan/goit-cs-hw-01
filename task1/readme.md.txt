Взагалі, я нічого не змінював у коді, бо в прикладі нам даний вираз a+b-c, а в завданні - b-c+a, що буквально те саме.

Якщо в завданні малось на увазі переставити пару рядків, щоб йти в "вірному порядку" при підрахунках, то виглядало б це так:

_start:
    mov al, [b]          ; Завантажуємо b в al
    sub al, [c]          ; Віднімаємо c від al
    add al, [a]          ; Додаємо a до al